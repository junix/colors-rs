# render determinism proof — chromap-explainer (rebuilt 2026-09-06 after the
# type-floor refine: all SVG text >=11px, CJK >=12px; page 8984 CSS px)
# Real two-run comparison: run1 stitched, then the whole render/ dir moved aside and a
# second independent browser session re-rendered from scratch; both full bitmaps and
# every slice compared. (The old proof rendered run2 to /tmp but hashed render/ —
# a no-op comparison; corrected earlier, VERIFICATION §3.6 item 13.)
# commands:
#   node shoot.js "file://$PWD/index.html" render && python3 stitch.py
#   mv render render.run1
#   node shoot.js "file://$PWD/index.html" render && python3 stitch.py
#   shasum -a 256 render.run1/full@2x.png render/full@2x.png
#   for f in render.run1/slice-*.png; do cmp "$f" "render/${f##*/}"; done   # all identical
#   rm -rf render.run1
page css: 1200x8984  dpr=2  slice=3600   (identical in both runs)
bitmap: 2400x17968  (stitch.py asserted height == cssHeight x dpr in BOTH runs)
run1 full@2x.png sha256: 47fb84939b28c0a966f3f455832e6b23a2cce98ee2e3749a8e6dfb7c2d42ece5
run2 full@2x.png sha256: 47fb84939b28c0a966f3f455832e6b23a2cce98ee2e3749a8e6dfb7c2d42ece5
slices: 3/3 byte-identical (cmp exit 0 for slice-00000/03600/07200)
deterministic: true
